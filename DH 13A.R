population <- c(500000,700000,600000)
temperature <- c(75,68,80)

plot(population,
     temperature,
     pch=19,
     col="red",
     main="Population vs Temperature",
     xlab="Population",
     ylab="Temperature")